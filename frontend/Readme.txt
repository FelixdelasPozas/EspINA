
CMake Configuration:
To compile our application we need the development files of ParaView
//TODO: Minimize Dependencies!!!
- ParaView_DIR: paraview/build
- It needs to generate the libpqCore.so (OSParaView server only project doesn't create it...)
