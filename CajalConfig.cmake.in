# Find the Cajal includes and libraries
#
# Once done, this will define: 
# CAJAL_FOUND: Cajal libraries are available
# CAJAL_INCLUDE_DIR: The Cajal include directories
# CAJAL_USE_FILE: File to include Cajal directories

# Name conventions: Dependencies use plural forms, 
# the package itself uses the singular forms defined
# by find_path and find_library


set (CAJAL_FOUND 1)

set (CAJAL_INCLUDE_DIR "@CAJAL_INCLUDE@")

set (CAJAL_LIBRARIES "@CAJAL_DATA_LIBRARY@" "@CAJAL_FILTERS_LIBRARY@")

set (CAJAL_USE_FILE "@CAJAL_USE_FILE@")
